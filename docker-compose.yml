version: '3.7'

services: 
  auth:
    image: joshuapn/auth
    build: 
      context: . 
  proxy:
    image: joshuapn/proxy
    build: 
      context: ./proxy
    ports: 
      - "80:8000"
    depends_on: 
      - auth 
